{
  "name": "pi-subagents",
  "version": "0.1.0",
  "type": "module",
  "description": "Task-first child delegation for Pi: foreground runs, background completion notices, and bounded subprocess lifecycle.",
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "subagents",
    "agents",
    "background"
  ],
  "license": "MIT",
  "author": "Nick Naru <nick@nijaru.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/nijaru/pi-subagents.git"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "test": "bun test",
    "lint": "oxlint extensions/ tests/",
    "typecheck": "tsc --noEmit",
    "check": "bun test && oxlint extensions/ tests/ && tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1",
    "@types/bun": "latest",
    "oxlint": "^1.81.0",
    "typebox": "^1.3.0",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "files": [
    "extensions",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22.19.0"
  }
}
